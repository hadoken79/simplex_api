version: "3"
services:
  node_dev:
    build: .
    container_name: node-docker
    restart: always
    ports:
      - 80:3000
      - 8080:8080
    volumes:
      - /home/hadoken/run:/home/app/storage
      #- ./:/home/app
      #- /media/linus/WEBARCHIV:/home/app/storage #mount local HDD to Server
      # example of windowsmount
      #- E:\\:/home/app/storage
    
    #stdin_open: true
    #tty: true
    