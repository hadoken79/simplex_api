module.exports = {
    add: (a, b) => {
     
       return parseInt(a) + parseInt(b);
    },
    comp: (a, b) => {
      if(a == b){
          return true;
      } else {
          return false;
      }
    }
  }